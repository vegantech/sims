"Student", "Date", "User Name", "Team Note"
<%= raw(::CSV.generate do |csv|
  @reporter.team_notes.each do |note|
      csv << [
        note.student.to_s,
        note.created_at.to_date.to_s(:report),
        note.user.to_s,
        note.body
      ]
    end
  end
) %>
