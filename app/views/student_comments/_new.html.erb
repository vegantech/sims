<% form_for(@student_comment, :url=>"/student_comments.js", :html=>{:multipart => true, :target=>"upload_frame"}) do |f| %>
  <%= render :partial=>f %>
<% end %>
  

  <%= link_to_function 'cancel',{:href=> url_for(current_student)} do |page| 
    page['new_team_note'].replace_html(:partial=>'students/add_note_link')
    end  %>
