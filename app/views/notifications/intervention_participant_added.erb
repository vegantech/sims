<%= d = DateTime.now.strftime("%B %d, %Y") %>

Hello, <%=@participant.fullname%>:

The following intervention is scheduled to begin on <%= @interventions.first.start_date %>:

Intervention: <%= @interventions.first.title %>
Students:     <%= @interventions.collect{|i| link_to(i.student.fullname,edit_intervention_url(i))}.join(",") %>

Team:
  <% @participants.sort_by(&:role).each do |p| %>
  <%=p.role_title %>  <%= p.fullname %>
<% end %>


<%= @interventions.first.autoassign_message %>


Please login to SIMS to view intervention details and enter student's progress monitoring data by clicking on one of the students above.


Thank you.
