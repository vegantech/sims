<% d = DateTime.now %><%= d.strftime("%B %d, %Y") %>

Dear <%= @user %>

<% @interventions.each do |intervention| %>

The following intervention is scheduled to end in one week:

Intervention: <%= intervention.title %>
Student:      <%= intervention.student %> 
End Date:     <%= intervention.end_date %>.

Please login to SIMS to view intervention details and update this student's progress monitoring data: 
<%= link_to intervention.title, url_for(@url_opts.merge(:controller=>'interventions', :action=>'edit', :id => intervention.id) ) %>

<% end %>


Thank you.


