<%= d = DateTime.now.strftime("%B %d, %Y") %>

Hello, <%=@override.student.principals.collect(&:fullname).uniq.join(",")%>:

<%=@override.teacher.fullname %> has requested a principal override for <%=@override.student.fullname %>.

The current tier is: <%=@override.start_tier.title %>.

The reason for this request is: <%= @override.teacher_request %>.

Please login to <%=link_to 'SIMS', root_url %> to accept or reject this override request.

Thank you.
