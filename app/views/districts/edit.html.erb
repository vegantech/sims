<h1>Editing district</h1>

<% form_for(@district, :html=> {:multipart => true}) do |f| %>
  <%= render :partial => f %>



  <p>
  <%= f.file_field :logo %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Back', root_url %>
