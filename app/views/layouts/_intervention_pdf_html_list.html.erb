<% plus_minus_li  "Intervention and Progress Monitors" do   %>
  <% Dir.glob("#{Rails.root}/public/system/district_generated_docs/#{current_district.id}/*.{pdf,html}").each do |file| %>
    <%= content_tag :li, link_to_with_icon(file.split("/").last, file.split("public",2).last) %>
  <% end %>
<% end %>


