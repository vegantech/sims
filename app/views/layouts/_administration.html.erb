<%= render :partial => 'administration/system' if can? :create, Country %>
<%= render :partial => 'administration/country' if can? :edit, current_district.country %>
<%= render :partial => 'administration/state' if can? :edit, current_district.state %>
<%= render :partial => 'administration/school' if can? :edit, current_school %>
<%= render :partial => 'administration/district' if can? :edit, current_district %>
