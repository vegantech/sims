<% if user_signed_in? && current_district && current_district.admin? %>
  <%= render_cell(:administration, :system, :district=>current_district, :user=>current_user) %>
<% end %>
  <%= render_cell(:administration, :school, :school => current_school,:user => current_user) if user_signed_in? && session[:school_id]%>
  <%= render_cell(:administration, :district, :district=>current_district ,:user => current_user) if user_signed_in?%>
