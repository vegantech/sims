<h1>Edit Probe </h1>
<% form_for @probe, :url=>probe_url(@intervention,@intervention_probe_assignment,@probe), :method=>:put do |f| %>
  <%=render :partial=>f %>
 <p>
    <%= f.submit "Enter Score" %>
  </p>
<% end %>

<%= link_to 'Back', probes_path(@intervention,@intervention_probe_assignment) %>
