<% form_for :intervention_definition, :url => select_interventions_definitions_path(@goal_definition,@objective_definition,@intervention_cluster) do |f| %>
  <%= f.collection_select(:id, @intervention_definitions,:id,:title) %>
  <%= content_tag :noscript, submit_tag("Choose Goal") %>
  <%= submit_tag("Choose Intervention") %>
<% end %>
