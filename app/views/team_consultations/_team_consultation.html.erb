<% content_tag_for :li, team_consultation  do %>
<%= link_to_remote_degrades "#{team_consultation.requestor} on #{ team_consultation.created_at.to_date }" , {:url =>team_consultation, :method => :get} %>
<% end %>
