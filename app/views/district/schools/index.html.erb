<h1>Listing schools</h1>

<table>
  <tr>
  </tr>

<% for schools in @schools %>
  <tr>
    <td><%= link_to 'Show', schools %></td>
    <td><%= link_to 'Edit', edit_schools_path(schools) %></td>
    <td><%= link_to 'Destroy', schools, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New schools', new_schools_path %>
