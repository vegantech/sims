<h1>Listing roles</h1>

<table>
  <tr>
    <th>Name</th>
    <th>District</th>
    <th>State</th>
    <th>Country</th>
    <th>System</th>
    <th>Position</th>
  </tr>

<% for role in @roles %>
  <tr>
    <td><%=h role.name %></td>
    <td><%=h role.district %></td>
    <td><%=h role.state %></td>
    <td><%=h role.country %></td>
    <td><%=h role.system %></td>
    <td><%=h role.position %></td>
    <td><%= link_to 'Show', role %></td>
    <td><%= link_to 'Edit', edit_role_path(role) %></td>
    <td><%= link_to 'Destroy', role, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New role', new_role_path %>
