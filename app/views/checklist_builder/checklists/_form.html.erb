<p>
  Check to make this the active checklist: <%= f.check_box :active %>
</p>
<p>
  Text: <%=markdown_note %><br />
  <%= f.text_area :text %>
</p>
<p>
  Directions: (Required) <%=markdown_note %><br />
  <%= f.text_area :directions %>
  </p>

  Assigned Recommendation (Blank will mean the checklist is optional: <br />
  <%= f.collection_select(:recommendation_definition_id, @checklist_definition.district.recommendation_definitions,:id,:text, :include_blank=>true) %>

