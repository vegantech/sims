page.insert_html(:after, "#{dom_id(@consultation_form)}", :partial=>"edit")
page.hide("show_hide_view_#{dom_id(@consultation_form)}")
page.hide("edit_#{dom_id(@consultation_form)}")
show_id = "show_#{dom_id(@consultation_form)}"
page<< "if($('#{show_id}')){"
page[show_id].hide
page << "}"
