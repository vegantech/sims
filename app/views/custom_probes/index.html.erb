<h1>Listing custom_probes</h1>

<table>
  <tr>
  </tr>

<% for custom_probe in @custom_probes %>
  <tr>
    <td><%= link_to 'Show', custom_probe %></td>
    <td><%= link_to 'Edit', edit_custom_probe_path(custom_probe) %></td>
    <td><%= link_to 'Destroy', custom_probe, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New custom_probe', new_custom_probe_path %>
